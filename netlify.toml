[build]
  command = "npm run build"
  publish = ".next"

[[headers]]
  for = "/_next/static/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

# Disable secret scanning - these are PUBLIC keys intentionally exposed to client
[build.environment]
  SECRETS_SCAN_ENABLED = "false"